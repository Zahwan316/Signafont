import { BrowserRouter, Route, Router, Routes } from "react-router-dom"

const RouteList = () => {
    return(
        <Routes>
            <BrowserRouter>
                <Route> </Route>
            </BrowserRouter>
        </Routes>
    )
}

export default RouteList